# Local Postgres (matches docker-compose). For Vercel, paste the Neon
# pooled connection string here instead.
DATABASE_URL=postgres://babytube:babytube@localhost:5432/babytube

# Admin password for /admin (change me in production)
ADMIN_PASSWORD=babygirl123

# Server-side only — never expose to the client
YOUTUBE_API_KEY=
